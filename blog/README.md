# Blog publishing

1. Add the post data to `../../content.js`.
2. Copy one of the existing post folders and rename it to the post slug.
3. Update its `data-slug` value to match the new entry exactly.

The small route file provides the clean URL; all post text is rendered from `content.js`.
